tasks:
  -
    name: install
    default: true
    language: Bash
    code: |
      sudo npm install -g stepci
    depends:
      -
        name: install-deps
  -
    name: install-deps
    language: Bash
    code: |
      sudo apk add --update nodejs npm
