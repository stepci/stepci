version: "1.1"
name: Validating HTML
tests:
  example:
    steps:
      - name: GET request
        http:
          url: https://example.com
          method: GET
          check:
            selector:
              title: Example Domain
